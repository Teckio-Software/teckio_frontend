export interface partidasDTO{
  id: number;
  descripcion: string;
  observaciones: string;
  nivel: number;
}

export interface partidasCreacionDTO{
  descripcion: string;
  observaciones: string;
  nivel: number;
}
